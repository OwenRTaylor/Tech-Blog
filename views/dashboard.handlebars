<h2>Dashboard</h2>
{{#each posts as |post|}}
<article class="post">
<a href="{{post.post_url}}"><h3>{{post.title}}</h3></a>
<h4>{{post.post_url}}</h4>
CREATED AT {{post.created_at}}
</article>
{{/each}}
